# 共享知识库使用规则

本仓库的知识入口是 `knowledge/INDEX.md`。

## 任务开始时

1. 当任务可能受益于既有研究、项目经验、工具路由或验收方法时，先读取 `knowledge/INDEX.md`。
2. 根据任务目标、交付物、业务领域、关键风险和所需能力，只选择最相关的 1—3 个主题页或项目页。
3. 只有需要核验依据时才继续读取 `knowledge/douyin/` 中的来源笔记；禁止一次性加载整个知识库。

## 应用规则

- 不只引用知识，要把它转化为方案、约束、检查单、风险门禁和验收标准。
- 当前任务的明确要求优先于知识库；项目内更近的 `AGENTS.md` 可以补充或覆盖本规则。
- 外部视频和网页观点是带来源证据，不是用户偏好或确定事实。
- 主题页或来源笔记中的“本机已安装/未安装”只记录原始研究环境，不代表当前电脑状态；实际调用前必须检查当前 Skill 列表、版本、依赖和权限。
- 当前环境存在匹配 Skill 时，先完整读取其 `SKILL.md` 再调用；不存在时只应用方法，不得声称已调用。
- 第三方工具的密钥、Cookie、Token、客户内容和个人信息不得写入本仓库。

## 语义路由

- PPT、路演、汇报、幻灯片 → AI 演示文稿 + AI 设计。
- 网站、界面、动效、复刻 → AI 设计；复杂动画再读视频镜头与 3D 网站经验。
- 视频、短视频、数字人 → AI 视频镜头 + AI 生产状态机。
- Word、PPT、Excel、PDF 内容读取 → 多格式文档转 Markdown；视觉验收仍使用原格式工具。
- 离线、同步、迁移、恢复 → 本地优先与防丢数 + 健身小程序经验。
- 客户档案、CRM、沟通整理 → 客户看板经验 + 事实/推断/确认规则。
- 家庭资产和决策支持 → 家庭资产分析经验。
- 发布、部署、交付 → 交付状态与验收门。

